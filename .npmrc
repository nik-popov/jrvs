# wrangler@4.x optionally peers @cloudflare/workers-types@^5 while
# agents→partyserver peers ^4; both work at runtime. Relax npm's strict
# peer resolution (the upstream cloudflare/agents monorepo uses pnpm).
legacy-peer-deps=true
